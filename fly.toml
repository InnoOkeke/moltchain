# Moltchain Agent - Fly.io Configuration
app = "moltchain"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  BASE_RPC_URL = "https://mainnet.base.org"

# No process overrides - use Docker CMD
[processes]

# Machine configuration
[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

# Rolling deployment strategy
[deploy]
  strategy = "rolling"
